<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlobalAudit">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.GlobalAudit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Intent filter to handle the ADFS redirect -->
            <!-- TODO: Once http://global-audit is registered in ADFS, update to use global-audit -->
            <intent-filter
                android:autoVerify="true"
                android:label="adfs_redirect_handler"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Using sahlaapp redirect URI (same as efulfill-android) for now -->
                <data android:scheme="http" android:host="sahlaapp" />
                <data android:scheme="https" android:host="sahlaapp" />
                <data android:scheme="http" android:host="Sahlaapp" />
                <data android:scheme="https" android:host="Sahlaapp" />
                <data android:scheme="http" android:host="SahlaApp" />
                <data android:scheme="https" android:host="SahlaApp" />
                <!-- For future use when global-audit is registered -->
                <data android:scheme="http" android:host="global-audit" />
                <data android:scheme="https" android:host="global-audit" />
            </intent-filter>
        </activity>
    </application>

</manifest>